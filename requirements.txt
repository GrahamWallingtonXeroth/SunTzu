# Core Flask web framework and extensions
Flask==3.0.2                    # Main web framework - stable version compatible with Python 3.12
flask-cors==4.0.0               # Cross-Origin Resource Sharing support for Flask APIs
Werkzeug==3.0.1                 # WSGI web application library (Flask dependency)
Jinja2==3.1.3                   # Template engine for Flask (Flask dependency)
MarkupSafe==2.1.5               # Safely add HTML/XML markup (Jinja2 dependency)
blinker==1.7.0                  # Fast dispatching of signals to named receivers (Flask dependency)
click==8.1.7                    # Command line interface creation kit (Flask dependency)
itsdangerous==2.1.2             # Various helpers to pass trusted data to untrusted environments (Flask dependency)

# Scientific computing and data processing
numpy==2.0.0                    # Fundamental package for scientific computing with Python
noise==1.2.2                    # Perlin noise generation for procedural map generation

# Testing framework
pytest==8.0.0                   # Testing framework for Python applications
pytest-cov==4.1.0               # Coverage reporting plugin for pytest
pytest-flask==1.3.0             # Flask testing utilities for pytest

# Development and debugging tools
Pygments==2.17.2                # Syntax highlighting package (used by Flask debugger)

# Utility packages
packaging==24.0                  # Core utilities for Python packages
pluggy==1.4.0                   # Plugin and hook calling mechanisms for Python
iniconfig==2.0.0                # INI file parser (pytest dependency) - fixed invalid version

# Google Cloud Platform specific considerations:
# - All packages are compatible with Python 3.12 runtime
# - Versions are stable and well-tested in production environments
# - No packages that require system-level dependencies or compilation
# - Flask 3.0.2 is the latest stable version with full Python 3.12 support
# - numpy 2.0.0 is optimized for Python 3.12 and modern architectures
# - All dependencies are pure Python or have pre-compiled wheels for App Engine
# - iniconfig 2.0.0 is the latest valid version compatible with Python 3.12
